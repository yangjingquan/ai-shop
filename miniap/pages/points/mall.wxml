<view class="mall page-shell"><view class="balance-bar">当前可用积分 <text>{{balance}}</text><view class="address-link" bindtap="chooseAddress">{{addressId ? '已选地址' : '选择收货地址'}} ›</view></view><view class="notice">实物兑换纯积分支付，商家包邮</view><view class="grid"><view wx:for="{{items}}" wx:key="id" class="item card"><image wx:if="{{item.image}}" class="image" src="{{item.image}}" mode="aspectFill"/><view wx:else class="image placeholder">积</view><view class="name">{{item.title}}</view><view class="bottom"><view class="price">消耗 {{item.pointsPrice}} 积分</view><button class="redeem {{!item.canRedeem ? 'disabled' : ''}}" disabled="{{!item.canRedeem || redeemingId === item.id}}" bindtap="redeem" data-item="{{item}}">{{redeemingId === item.id ? '兑换中' : item.redeemText}}</button></view><view class="stock">{{item.stockText}}</view><view class="quota">{{item.quotaText}}</view></view></view><view wx:if="{{!items.length}}" class="empty-state"><view class="empty-title">暂无可兑换好礼</view><view class="empty-desc">商家正在准备中</view></view></view>
