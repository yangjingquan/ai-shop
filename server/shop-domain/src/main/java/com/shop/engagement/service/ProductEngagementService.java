package com.shop.engagement.service; import com.shop.engagement.dto.*; import com.shop.common.response.PageResult; public interface ProductEngagementService {EngagementVO detail(Long userId,Long merchantId,Long productId);void recordHistory(Long userId,Long merchantId,Long productId);void favorite(Long userId,Long merchantId,Long productId,boolean value);PageResult<EngagementVO.ProductRecord> favorites(Long userId,Long merchantId,int page,int size);PageResult<EngagementVO.ProductRecord> histories(Long userId,Long merchantId,int page,int size);void removeHistory(Long userId,Long merchantId,Long productId);void clearHistory(Long userId,Long merchantId);Long review(Long userId,Long merchantId,EngagementRequests.Review request);Long question(Long userId,Long merchantId,EngagementRequests.Question request);PageResult<EngagementVO.Review> myReviews(Long userId,Long merchantId,int page,int size);PageResult<EngagementVO.Question> questions(Long merchantId,int page,int size);void answer(Long merchantId,Long userId,Long id,EngagementRequests.Answer request);void hideReview(Long merchantId,Long id,EngagementRequests.Hide request);void hideQuestion(Long merchantId,Long id,EngagementRequests.Hide request);}
