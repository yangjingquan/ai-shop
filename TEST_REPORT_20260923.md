# 商城系统测试报告（2026-09-23）

## 结论

**当前不能判定全系统验收通过，也不建议以本次结果作为上线放行依据。** 在隔离环境中，运营后台、商户后台和小程序公开接口之间的商家入驻、分类、商品审核上架与展示闭环已实际跑通；双商户数据隔离及商家冻结也通过。与此同时，新库初始化存在商户权限授予错误，后端测试基线未通过，小程序自动化入口缺文件，订单、支付、履约、退款等交易闭环尚无可执行的端到端验证。

本次测试是**当前代码和可用环境的实际执行结果**。所有写入仅发生在 `shop_codex_test_20260923` 隔离库；微信开发者工具中连接生产接口的小程序只进行了只读浏览及打开购买规格弹层，未提交订单或支付。

## 范围、环境与方法

| 项目 | 实施情况 |
|---|---|
| 代码基线 | Git `f330638`；测试开始时工作区干净。本报告是唯一新增文件，未修改业务代码。 |
| 服务 | Docker MySQL、Redis；隔离运营 API `127.0.0.1:8181`、小程序 API `127.0.0.1:8182`、后台 Vite `127.0.0.1:5181`。测试库 `shop_codex_test_20260923`，Redis DB 15。 |
| 测试账号 | 隔离库运营管理员、原有测试商家，以及本次新增的二号测试商家；未在报告记录密码。 |
| 执行方式 | Chrome 中按角色操作真实后台页面；微信开发者工具中检查小程序模拟器；调用隔离小程序公开接口；核对路由、页面文件、事件绑定和权限迁移；执行构建与 Maven 测试。 |
| 页面覆盖 | 运营后台 14/14 个注册路由、商户后台 28/30 个注册路由做桌面端加载/可达性抽查；小程序 `app.json` 注册的 39 页做文件完整性和事件绑定静态检查，模拟器实际查看首页、分类、购物车、商品详情 4 页。 |
| 付款范围 | 未做真实支付。支付、退款、对账的独立模拟单测通过；小程序登录至支付的完整模拟交易没有可用测试链路，见“未完成验收范围”。 |

后台路由抽查只证明页面可进入、主体内容能渲染、桌面视口未发现整页横向溢出，**不代表每个页面内的所有按钮、弹窗、移动端适配和业务规则都已通过**。空库中订单、支付、退款、营销、结算等页面多为空状态，已核对页面结构，但没有对应真实交易供操作。

## 实际执行结果

| 用例 | 操作与观察 | 结果 |
|---|---|---|
| 双后台登录与角色隔离 | 运营、商家均可登录；运营访问商家路由、商家访问运营路由均被重定向至各自首页。 | 通过 |
| 商家入驻与状态 | 运营端“新增商家”空表单显示三项必填提示；提交二号商家后列表出现并可登录。冻结后，原会话进入商品页被导向登录页，再次登录显示“商家已被冻结”；重新启用成功。 | 通过 |
| 平台分类到商户分类 | 运营新增“系统测试文具”；商户从平台导入，显示来源“平台导入”，再创建子类“测试钢笔”。 | 通过 |
| 商品创建、审核、上架 | 商户选子类、包邮模板、SKU（黑色，库存 10）创建“系统测试钢笔”（¥15）；创建后为待商家审核且下架；审核通过并上架后列表状态正确。 | 通过，依赖测试库临时补授权限 |
| 运营监管与小程序公开展示 | 运营商品审核页可见商品、商户、分类、库存和审核状态；小程序公开商品分页、详情、分类树、首页接口返回成功，商品分页显示该商品及 ¥15/库存 10。 | 通过 |
| 租户隔离 | 一号商家公开商品列表 `total=1`，二号商家 `total=0`；二号商家以商品 ID 直接请求一号商品详情得到业务码 112“商品不存在”；二号商家后台商品页显示“暂无商品”。 | 通过 |
| 操作日志 | 商户审核商品后，运营操作日志出现 `MERCHANT_PRODUCT_AUDIT` 记录。 | 通过（仅此动作） |
| 小程序模拟器 | 首页、分类、购物车空状态与推荐商品、商品详情可显示；“立即购买”可打开规格选择层。 | 部分通过；未提交订单 |
| 后台构建 | `admin` 的 `npm run build` 通过；`server` 的 `./mvnw -q -DskipTests package` 通过。 | 通过 |
| 后端回归 | 隔离库执行 `./mvnw -q test`：175 项，133 通过、3 断言失败、39 报错、0 跳过，构建失败。 | 未通过 |
| 小程序 E2E 命令 | `pnpm test:e2e` 指向不存在的 `tests/e2e/smoke.js`，报 `MODULE_NOT_FOUND`。 | 未通过 |

> 说明：新库中店主缺少运费模板权限，原本商品编辑页获取模板时报“无权限”，运费模板路由也重定向。经用户明确批准，仅向隔离库的一号商家店主补授 `merchant:freight:view` 与 `merchant:freight:manage` 后，才完成商品闭环。该临时补授没有修复代码或其他商户，不能视为默认安装流程通过。

## 问题清单

### T-20260923-01 · P1 · 新库商户权限初始化错误

- **复现**：全新隔离库登录默认商家店主，进入“运费模板”会被路由守卫重定向；进入新增商品页时读取模板报“无权限”。权限表有 `merchant:freight:view/manage`，但店主和运营人员均无关联。
- **原因证据**：[V72__freight_templates.sql](/Users/yangjingquan/Documents/test/shop/server/shop-domain/src/main/resources/db/migration/V72__freight_templates.sql:46) 以 `MERCHANT_ADMIN`、`MERCHANT_OPERATOR` 授权；真实内置角色代码在 [V39__merchant_rbac.sql](/Users/yangjingquan/Documents/test/shop/server/shop-domain/src/main/resources/db/migration/V39__merchant_rbac.sql:84) 是 `owner`、`operator`。同类错误也出现在 [V73__product_engagement.sql](/Users/yangjingquan/Documents/test/shop/server/shop-domain/src/main/resources/db/migration/V73__product_engagement.sql:23)，使商品互动管理权限未授予这两类角色。
- **影响**：在 V72/V73 执行前已存在的商家角色拿不到新增权限，物流模板管理不可用；商品创建依赖模板读取，影响上新。本次迁移后通过运营后台新建的二号商家可进入运费模板页，说明新商家创建流程另有权限初始化逻辑。其他环境需按迁移历史核对。
- **建议**：增加新的修复迁移，按实际角色代码回填两版权限，并为新商家角色初始化增加回归测试；修复后用全新库复测，不依赖手工补授。

### T-20260923-02 · P1 · 后端回归基线未通过

- **复现**：隔离库运行 `./mvnw -q test`，共 175 项，3 Failures、39 Errors。失败涉及库存预占、超时拼团订单取消、团购商品校验；报错集中于“分类不存在”“商品不存在”“商户微信支付配置未完成”等。
- **定位**：多项集成测试依赖固定的分类/商品 ID 或预置支付配置，空白迁移库未提供相应 fixture；3 项断言失败还需独立排查，不能直接归因于缺 fixture。首次在受限沙箱中运行又遇到 MySQL 连接失败，随后在可访问隔离库的环境中重跑得出上述最终结果。
- **建议**：让每个测试显式创建自身数据，或提供版本化测试 fixture；修复三项断言；要求完整套件在干净库、CI 中 0 失败、0 报错后再作为回归门禁。

### T-20260923-03 · P2 · 小程序 E2E 入口失效

- **证据**：[package.json](/Users/yangjingquan/Documents/test/shop/miniap/package.json:6) 的 `test:e2e` 和 `test:e2e:smoke` 均指向 `tests/e2e/smoke.js`，但当前目录为空。执行直接报 `MODULE_NOT_FOUND`。此前的 2026-09-13 报告提到该脚本，当前代码基线已不存在。
- **影响**：39 个注册页面和关键按钮无法由现有命令自动巡检。
- **建议**：恢复并维护可运行的 E2E 脚本，至少覆盖五个 Tab、商品、购物车、订单与支付模拟，并在 CI 中运行。

### T-20260923-04 · P2 · 新人券弹窗绑定不存在的处理函数

- **证据**：[首页 WXML](/Users/yangjingquan/Documents/test/shop/miniap/pages/home/index.wxml:66) 的弹窗容器使用 `catchtap="noop"`，首页 JS 中没有 `noop` 方法。本次静态核对 39 页事件处理函数时，仅发现这一处缺失。
- **影响**：新人券弹窗出现并点击容器时可能产生微信小程序事件处理错误；该条件未在模拟器中触发，因此尚未实测错误表现。
- **建议**：定义空处理函数或采用无需绑定缺失函数的阻止冒泡方式，并补弹窗点击测试。

### T-20260923-05 · P3 · 场景文案不一致

- [分类页](/Users/yangjingquan/Documents/test/shop/miniap/pages/category/index.wxml:4) 搜索提示仍为“手机、香氛、运动鞋”；[后台登录页](/Users/yangjingquan/Documents/test/shop/admin/src/views/Login.vue:66) 仍显示 `Premium Tote`、`Daily Aroma` 示例。与当前文具商品场景不一致，影响页面可信度。

## 未完成验收范围与原因

| 范围 | 当前证据与缺口 |
|---|---|
| 小程序全页与按钮 | 39 页仅 4 页经过模拟器实际查看；其余页面只完成注册文件与事件绑定静态检查。首页新人券、地址、优惠券、积分、拼团、秒杀、预售、评价、提问、收藏、足迹等页面未逐一点击。 |
| 订单到售后全链路 | 未完成“登录/加购/地址/下单/模拟支付/回调/库存/发货/收货/退款/结算/对账”端到端交易。当前小程序默认访问生产 API；隔离 API 的微信登录需要真实 `jscode2session` 配置，项目没有可运行的 E2E 支付 mock 测试入口。不能从独立单测通过推断端到端支付已通过。 |
| 营销与账务 | Banner、优惠券、积分、拼团、秒杀、预售、满减、结算、对账的后台页面可达，但隔离库缺完整交易和活动数据，未实操活动配置到前台下单、退款的闭环。 |
| 视觉与兼容性 | 后台仅做桌面视口路由与整页溢出抽查；小程序只做单一模拟器中的 4 页可见性检查。未完成真机 iOS/Android、不同尺寸、弱网、字体放大、读屏、图片上传或全页面截图比对。 |
| 安全与性能 | 仅验证角色路由隔离、商品列表/详情租户隔离及冻结会话。未做并发抢购、重复支付回调、接口越权矩阵、压力或渗透测试。 |

## 后续验收条件

1. 修复 T-20260923-01 后，用**全新库**重复店主/运营人员的运费模板与商品上新测试，确认无需手工授权。
2. 修复 T-20260923-02、03，确保后端测试和小程序 E2E 在 CI 中可重复执行并通过。
3. 提供隔离小程序登录与支付模拟环境，使用同一套隔离商家数据完成普通订单及至少一类营销订单的支付、发货、确认收货、退款、库存和对账闭环，并核对运营、商户、小程序三端状态一致。
4. 对剩余小程序页面、后台各页面的增删改查与异常路径逐项执行；补真机、多尺寸、弱网和可访问性专项检查。

## 可复核材料

- 后端本次执行日志：`/private/tmp/shop-system-test-maven-20260923.log`（本机临时文件）；详细报告位于 `server/shop-domain/target/surefire-reports/`。日志最终汇总为 `Tests run: 175, Failures: 3, Errors: 39, Skipped: 0`。
- 隔离库保留测试数据：二号商家 `M3E6BNX3Y9V`（最终状态“启用”）、平台/商户分类与商品 `系统测试钢笔`。隔离服务在测试结束后停止；重新启动服务即可复核数据。
- 支付与退款的独立单测类 `WxPayServiceImplTest`、`OrderPaymentServiceImplTest`、`PaymentReconciliationServiceImplTest`、`RefundCompletionServiceImplTest`、`RefundReconciliationServiceImplTest`、`OrderStateMachineTest` 在本次 Maven 执行中各自无失败，但它们不覆盖完整的小程序交易链路。
